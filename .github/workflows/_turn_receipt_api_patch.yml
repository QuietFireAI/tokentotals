name: Turn Receipt API patch work

on:
  push:
    branches:
      - turn-receipt-api-work

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm branch workflow execution
        run: echo "turn-receipt-api-work helper is executable"
